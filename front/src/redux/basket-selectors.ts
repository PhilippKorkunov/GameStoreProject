import {RootState} from "./store";
import {IGame} from "./basket-reducer";

export const basketSelector = (state: RootState) => {
  // let basketItems: IGame[] = [];
  // state.basket.items.forEach((item) => {
  //   basketItems.push({
  //     "id": item.id, "src": item.src,
  //     "title": item.title, "platform": item.platform, "price": item.price,
  //   });
  // });
  // return basketItems;
}